# Release v2.2.0

- Date (UTC): 2026-09-06
- Previous tag: v2.1.4
- Channel: stable
- Stage: stable

## Highlights

- AI 发现（discovery）：新增 `/llms.txt`（含分页）、各内容域 `index.md` 与 `llms.txt`、`sitemap.xml` / `sitemaps/*` 与 `robots.txt` 动态端点，配套后台「站点信息」分组配置与迁移 `0070`
- 内容导出：支持将内容导出为 markdown 归档并打包引用图片
- 页脚：桌面端列数随分组数自适应（品牌块保证最小宽度），新增可配置的「如果你是 Agent >」导航分组
- 修复：数字滚动组件复制时输出实际数值而非全部数字格

## Commits

- feat(footer): adaptive columns and configurable agent discovery section (e3d9887)
- fix(ui): make rolling numbers copy the actual value instead of all digit cells (52cb9f8)
- feat(discovery): serve llms.txt, sitemaps and robots.txt for AI discovery (f4d4eea)
- feat(content-export): export content as markdown archive with bundled images (09048d9)
